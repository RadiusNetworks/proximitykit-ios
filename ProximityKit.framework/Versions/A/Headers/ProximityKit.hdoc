/*! @framework Proximity Kit Framework Reference
    @abstract
    @discussion The Proximity Kit framework provides integration with
    the Proximity Kit service and manages GeoFence and iBeacon events.

    It defines a manager class (PKManager) as well as a protocol for defining
    the callbacks it supports (PKManagerDelegate).

    @frameworkpath ProximityKit.framework
 */

